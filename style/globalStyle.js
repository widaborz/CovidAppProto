import { StyleSheet } from 'react-native';

export const globalStyles = StyleSheet.create({
    title: {
        fontSize: 15, 
        fontWeight: "bold", 
        color: "#fff"
      }, 
      blackTitle: {
        fontSize: 15, 
        fontWeight: "bold", 
        color: "#000"
      }, 
      colorBright: {
        color: "#fff"
      },
});